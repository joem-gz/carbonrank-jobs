# CLAUDE.md

Follow the repo’s agent rules in `AGENTS.md` (single source of truth).

If `AGENTS.md` conflicts with a user instruction, pause and record the conflict + options in the task note before proceeding.
